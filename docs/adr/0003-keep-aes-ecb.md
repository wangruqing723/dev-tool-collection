# 保留 AES-ECB 模式

AES 命令同时提供 CBC、GCM、ECB 三种模式。ECB 在密码学上是有缺陷的——相同明文块产出相同密文块，会泄露明文结构——设计时曾建议不予提供，经权衡后仍决定保留：本工具的用途是对接既有系统，而国内部分老接口确实只用 AES-ECB。工具不支持并不会让那些接口变安全，只会把用户推向更不可控的替代工具。

## Consequences

ECB 在模式下拉文案中标注「不安全，仅用于对接老系统」，选中时额外展示风险说明，且不显示 IV 字段。这是刻意保留的能力，不是疏漏——请勿以「安全加固」为由移除。
