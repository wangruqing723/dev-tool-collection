# 扩展间复制公共代码，不建共享包

`result.ts`、`guard.ts` 这类公共代码在各扩展间以**复制**方式存在，而非抽成共享包。本仓的三个扩展是各自独立发布到 Raycast Store 的单元，没有 workspace，引入共享包会带来构建顺序与版本联动成本，为几十行校验与提示函数不值得。

## Consequences

同一函数存在多份副本，修 bug 需逐个扩展同步。这是已知且接受的重复。若公共代码量显著增长（例如涉及核心加解密算法本身，而非边缘校验），再重新评估此决定。
