# Dev Tool Collection

一组各自独立发布的 Raycast 扩展，面向开发者的日常编解码与加解密操作。本文件只定义本项目自己的用语，不记录实现细节。

## Language

**可逆加解密**：
持有密钥即可从密文还原出明文的运算。是命令归属 `crypto-advanced` 扩展的唯一判据。
_Avoid_: 高级加密、强加密

**单向摘要**：
无法从输出还原出输入的运算，含令牌签发与校验。此类命令归属 `crypto-tool` 扩展。
_Avoid_: 不可逆加密、哈希加密

**原始密钥**：
直接作为算法密钥参与运算的字节串，由用户以 Hex 或 Base64 提供，长度即决定算法变体。本项目不接受口令派生密钥。
_Avoid_: 密码、口令、passphrase

**密钥历史**：
命令记住用户近期用过的密钥，供下次直接选取。仅对称密钥进入密钥历史，RSA 私钥永不进入。
_Avoid_: 密钥库、keystore、密钥管理

**自包含密文**：
将 IV/Nonce 与认证标签一并拼入的密文形态，使密文无需另外附带参数即可解密。
_Avoid_: 完整密文、打包密文
