# Dependencies
node_modules/

# Build outputs
dist/
build/
*.tsbuildinfo

# IDE
.vscode/
.idea/

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Environment variables
.env
.env.local
.env.*.local

.codegraph/

# 说明：package-lock.json 与 raycast-env.d.ts 必须入库，不要忽略。
# - package-lock.json：依赖全是 ^ 范围，不锁版本会让 Windows 机器 npm install
#   解析出与 macOS 不同的传递依赖版本。
# - raycast-env.d.ts：三个 tsconfig 的 include 都显式引用它，缺失会导致
#   全新克隆后 npm run type-check 直接报错。
